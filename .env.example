# Production (Vercel) : URL publique de l’API, avec /api en fin de chemin.
# VITE_API_BASE=https://figsapp-cote-divoire-backend.onrender.com/api
#
# Développement : laisser vide ou absent — Vite proxy vers localhost:5000 (voir vite.config.js).
