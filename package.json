{
  "name": "figsapp-cote-divoire",
  "version": "1.0.0",
  "private": true,
  "description": "FigsApp Côte d'Ivoire — frontend + API Vercel + Neon",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run vercel-build",
    "vercel-build": "npm --prefix backend install --omit=dev --no-optional && npm --prefix frontend install && npm --prefix frontend run build",
    "db:init": "cd backend && node --env-file=.env.neon.local database/ensurePostgresSchema.js",
    "db:seed-admin": "cd backend && node --env-file=.env.neon.local database/seed-admin-postgres.js",
    "db:seed-render": "cd backend && node database/seed-render-data-postgres.js"
  }
}
